# Local Development Environment
# Copy this file to .env: cp .env.example .env

NODE_ENV=development

# Database - Local Docker PostgreSQL
DATABASE_URL=postgresql://dev_user:dev_password@localhost:5432/cvstomize_dev?schema=public

# Redis - Local Docker Redis
REDIS_URL=redis://localhost:6379

# Server
PORT=3001

# CORS
CORS_ORIGIN=http://localhost:3000

# ─────────────────────────────────────────────────────────────────
# 💰 COST-FREE LOCAL DEVELOPMENT
# ─────────────────────────────────────────────────────────────────
# Mock AI: Set to 'true' to use mock responses (no GCP costs)
USE_MOCK_AI=true

# Firebase Emulator: Uncomment to use local auth emulator (no Firebase costs)
# Start emulator with: docker compose --profile emulators up
# FIREBASE_AUTH_EMULATOR_HOST=localhost:9099

# ─────────────────────────────────────────────────────────────────
# GCP/Vertex AI (for testing with real AI - costs money)
# ─────────────────────────────────────────────────────────────────
# Set USE_MOCK_AI=false and provide credentials:
GOOGLE_APPLICATION_CREDENTIALS=./gcp-key.json
GCP_PROJECT_ID=cvstomize

# Development Tools
DEV_ADMIN_MODE=true
