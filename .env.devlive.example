# DevLive Environment Variables
# Copy this to .env.devlive and fill in your values
# This file is gitignored for security

# GCP Project ID
GCP_PROJECT_ID=cvstomize

# Database URL - Choose one:
# Option 1: Cloud SQL (staging-like, requires Cloud SQL Proxy or public IP)
# DATABASE_URL=postgresql://cvstomize_app:PASSWORD@HOST:5432/cvstomize_staging?schema=public

# Option 2: Local database (uses docker-compose postgres)
DATABASE_URL=postgresql://dev_user:dev_password@database:5432/cvstomize_dev?schema=public

# Redis URL (optional - defaults to local container)
# REDIS_URL=redis://redis:6379
